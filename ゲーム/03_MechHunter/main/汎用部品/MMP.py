#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#┃共通処理
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
from . import mmp
from . import MMP

接続 = None  # MMPオブジェクト

#────────────────────────────────────    
def 初期化():
    #┬
    #●MMPを実体化する。
    MMP.接続 = mmp()
    #│
    #●MMPを接続する
    MMP.接続.通信接続_自動()
    #│
    #●アナログ入力を設定する
    MMP.接続.アナログ設定(
        1, # 使用するHC4067の個数(1～4)
        8, # 使用するHC4067のPin数(1～16)
        10 # アナログ値の丸め(この数値以下は切り捨て)
        )
    #┴
